{
  "name": "gotaskme",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.1"
  },
  "scripts": {
    "e2e:prepare": "yarn && npx playwright test --grep @prep",
    "e2e:cleanup": "yarn && npx playwright test --grep @cleanup",
    "test:e2e": "yarn && npx playwright test --grep @test"
  }
}
